/**
 * Résumé PDFs are generated from a single source of truth by
 * `npm run generate:resume` (see scripts/resume/content.mjs) and served as
 * static files. The site links the full-stack variant; the frontend-specialist
 * variant exists for applications to frontend-titled roles.
 */
export const RESUME_PDF = "/pdf/AbdulRehman_FullStack_Engineer_Resume.pdf";
